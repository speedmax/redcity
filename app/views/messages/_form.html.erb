<%= simple_form_for @message do |f| %>
	<%= f.input :label, :as => :select, :collection => ["Job", "Share", "News", "Question", "Event", "Meetup"] %>
  <%= f.input :content, :as => :text %>
  <%= f.button :submit %>
<% end %>
